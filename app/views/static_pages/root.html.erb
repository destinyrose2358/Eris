<script type="text/javascript">
  <% if logged_in? %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>;
  <% end %>
  window.logoURL = "<%= image_url('eris_logo.gif') %>"
</script>

<div id="root"></div>
